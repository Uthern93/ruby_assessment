<nav class="bg-white border-b border-gray-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
            <!-- Left side - Logo/Brand -->
            <div class="flex">
                <div class="flex-shrink-0 flex items-center">
                    <span class="text-xl font-semibold text-gray-800">Your App</span>
                </div>
            </div>

            <!-- Right side - Navigation -->
            <div class="flex items-center">
                <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
                
                    <%= link_to 'Homepage', 
                    root_path, 
                    class:'inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-900 border-b-2 border-indigo-500' 
                    %>

                    <%= link_to 'Create Blogs', 
                    home_create_path, 
                    class:'inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-500 hover:text-gray-700 hover:border-gray-300 border-b-2 border-transparent' 
                    %>

                    <%= link_to 'Profile', 
                    home_profile_path, 
                    class:'inline-flex items-center px-1 pt-1 text-sm font-medium text-gray-500 hover:text-gray-700 hover:border-gray-300 border-b-2 border-transparent' 
                    %>

                </div>
            </div>
        </div>
    </div>
</nav>