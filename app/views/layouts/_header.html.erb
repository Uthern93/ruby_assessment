<nav class="bg-white border-b border-gray-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">

            <div class="flex">
                <div class="flex-shrink-0 flex items-center">
                    <%= image_tag 'logo.png', alt: 'App Logo', class: 'h-9 sm:h-10 md:h-10' %>
                    <span class="text-sm text-violet-800 font-semibold text-gray-800 ml-2 sm:text-xl md:text-xl">Blogs App</span>
                </div>
            </div>

            <div class="flex items-center">
                <div class="sm:ml-6 sm:flex sm:space-x-8">

                    <%= link_to 'Homepage', 
                        root_path, 
                        class: "inline-flex items-center px-1 pt-1 text-xs md:text-base font-medium #{current_page?(root_path) ? 'text-gray-900 border-b-2 border-indigo-500' : 'text-gray-500 hover:text-gray-700 hover:border-gray-300 border-b-2 border-transparent'}" 
                    %>

                    <%= link_to 'Create Blogs', 
                        blogs_new_path, 
                        class: "inline-flex items-center px-1 pt-1 text-xs md:text-base font-medium #{current_page?(blogs_new_path) ? 'text-gray-900 border-b-2 border-indigo-500' : 'text-gray-500 hover:text-gray-700 hover:border-gray-300 border-b-2 border-transparent'}"
                    %>

                    <%= link_to 'Profile', 
                        home_profile_path, 
                        class: "inline-flex items-center px-1 pt-1 text-xs md:text-base font-medium #{current_page?(home_profile_path) ? 'text-gray-900 border-b-2 border-indigo-500' : 'text-gray-500 hover:text-gray-700 hover:border-gray-300 border-b-2 border-transparent'}"
                    %>
                </div>
            </div>
        </div>
    </div>
</nav>
