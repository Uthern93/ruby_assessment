<div class="max-w-2xl mx-auto mt-10">
  <h2 class="text-2xl font-bold mb-6 text-center">Edit Blog Post</h2>

  <%= form_with model: @blog, local: true, url: "/blogs/#{@blog.id}/edit", method: :patch do |form| %>
    <% if @blog.errors.any? %>
      <div class="bg-red-100 p-4 rounded mb-4">
        <ul>
          <% @blog.errors.full_messages.each do |msg| %>
            <li class="text-red-600"><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <% if @blog.image_url.attached? %>
      <div class="mb-4">
        <p class="block text-gray-700 font-semibold">Current Image:</p>
        <%= image_tag @blog.image_url, class: "mt-2 w-60 h-full object-cover rounded-md" %>
      </div>
    <% end %>

    <div class="mb-4">
      <%= form.label :title, class: "block text-gray-700 font-semibold" %>
      <%= form.text_field :title, class: "mt-1 w-full p-2 border border-gray-300 rounded" %>
    </div>

    <div class="mb-4">
      <%= form.label :content, class: "block text-gray-700 font-semibold" %>
      <%= form.rich_text_area :content %>
    </div>

    <div class="mb-4">
      <%= form.label :image, class: "block text-gray-700 font-semibold" %>
      <%= form.file_field :image_url, class: "mt-1 w-full border border-gray-300 rounded p-2 cursor-pointer bg-gray-100 hover:bg-gray-200", id: "imageInput" %>
    </div>

    <div class="flex justify-between mt-6">
      <%= link_to "Back", "/blogs/#{@blog.id}", class: "bg-gray-500 text-white font-semibold py-2 px-4 rounded hover:bg-gray-600" %>
      <%= form.submit "Update Blog", class: "bg-blue-600 text-white font-semibold py-2 px-4 rounded hover:bg-blue-700" %>
    </div>

  <% end %>
</div>
